# ALL-CHANGES

Curated master change log across all tracked CyberArk PAM API versions. Entries here are hand-picked highlights from `exports/comparison_*.md` runs — not a raw dump. Include only material API changes (endpoint added / removed / breaking shape change). Ignore documentation-only edits (wording, formatting, examples).

## Format

Each entry:

```
### <From> → <To>   (comparison_v<From>_to_v<To>_YYYYMMDD.md)

**Added**
- `<METHOD> <path>` — one-line purpose

**Removed**
- `<METHOD> <path>` — one-line purpose

**Modified (breaking)**
- `<METHOD> <path>` — what changed and why it breaks callers

**Modified (non-breaking)**
- `<METHOD> <path>` — what changed
```

---

## Entries

_No entries yet. Run a comparison and promote its highlights here._
