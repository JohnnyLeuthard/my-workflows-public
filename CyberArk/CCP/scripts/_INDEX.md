# CCP Scripts Index

No CCP scripts exist yet.

When scripts are added, document each script here with:

- Purpose
- Required inputs
- Whether it performs live CCP retrieval
- Output artifact path
- Secret handling behavior

Do not add scripts that write unredacted credential values to disk.
