# CCP Stage 04: Endpoint Testing

This stage is for approved live CCP endpoint tests.

Endpoint tests may retrieve secrets. Treat all outputs as sensitive and redact returned credential values before saving anything.
