# Jodi Paige-Lee — Debrief Specialist

Comp #8: The Wildcard. Your repo was read in full: [github.com/jmarielee/debrief-specialist](https://github.com/jmarielee/debrief-specialist) (commit 0b35cd66) · [live app](https://jmarielee.github.io/debrief-specialist/).

---

The sentence that carries your build is in Rule 8: a must in a markdown file is a request, a must in code is a constraint. Most people this month wrote that distinction as philosophy; you built both layers and then — this is the part I respect — named the boundary honestly in your own operating contract: the folder self-checks, the app enforces, state which one you are when you report. The publish gate in index.html is real code running the seven checks your rules describe, the planted trap name is a live falsifiability device, and the manual voice loop is the honest version of a folder that learns — visibly so, since the folder now holds three samples while the app's frozen prompt still has two. Your folder accreted past its own app. That's what the loop working looks like.

Two pushes. The check closest to your core principle — a number in the draft with no backing in the dump — only flags, never fails, so "clear to publish" can appear beside a fabricated metric; for you specifically that check should block. And your README's verification claims promise demo behavior the keyless demo can't show — the anonymization moment and the trap-name trip both need a live run, and the grep you suggest would actually return the trap name rather than come back empty. Tighten the claims to what a stranger can verify, ship an nda-protected.local.md.example, and note that the app's embedded prompt has to be regenerated when the folder changes — it's already drifted to "four finished structures" where the folder says five. Gabriel's check.py this week is the blocking version of your gate; you're one severity level away from it.

---

An idea worth naming, and it is yours: a must in markdown is a request, a must in code is a constraint — the two-layer architecture named as such, with a deliberately planted trap name so anyone can falsify the guarantee instead of trusting it.

Worth a look:
- [Gabriel Azoulay — Visit Ready](https://github.com/NFTYoginis/prepare-dr-visit) — the blocking gate: same seven-checks instinct, one severity level up
