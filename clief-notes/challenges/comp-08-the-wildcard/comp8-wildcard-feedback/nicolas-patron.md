# Nicolás Patrón Uriburu — structure-call-ar

Comp #8: The Wildcard. Your repo was read in full: [github.com/Nicopatron/structure-call-ar](https://github.com/Nicopatron/structure-call-ar) (commit 292260a5) · [landing](https://structure-call-ar.vercel.app/).

---

Your Rule 0 is this month's assignment stated as a design law: the deterministic trigger is mine to compute and commit to; the complex structural choice escalates to a named human. And you built both halves properly. The same arithmetic is pinned in calc.py's self-test, the TypeScript port's selfcheck, the worked examples, and the judge guide — I recomputed Lucía's numbers and they reconcile to the centavo across five documents and two codebases — and the live demo's validator degrades any output that claims high confidence on a decision that belongs to the contador. Escalation as a load-bearing section rather than a disclaimer, delivered as a Reviewer Brief the accountant can actually work from, turns the model's limit into the product. "Escalating once is signal; repeating it on every line is compliance theatre" deserves to be quoted in other people's rules files.

The push: your provenance chain's root is missing. Four reference files cite a research triangulation layer — cifras-fiscales, triangulacion — that isn't in the repo, so "triple-confirmed" is a claim I can't audit, in a build whose whole ethic is auditability. Commit it, even redacted. And 31KB of rules is a heavy contract for any runtime to obey; Matt H's one-page citable-rule-ID shape is the compression target.

---

An idea worth naming, and it is yours: the Reviewer Brief — the escalation converted into a working artifact for a named human, with the verified inputs, the computed trigger, and the open questions laid out the way the contador actually works. The disclaimer replaced by a deliverable.

Worth a look:
- [Matt H — Realee Lead Desk](https://github.com/Realeeai/realee-lead-desk) — the one-page ordered rubric with citable rule IDs; your compression target
