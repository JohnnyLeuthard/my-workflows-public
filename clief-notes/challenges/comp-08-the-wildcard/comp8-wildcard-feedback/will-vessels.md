# Will Vessels — content-engine

Comp #8: The Wildcard. Your repo was read in full: [github.com/wcvessels/content-engine](https://github.com/wcvessels/content-engine) (commit d0fd3802) · companion: [transcription-pipeline-plugin](https://github.com/wcvessels/transcription-pipeline-plugin).

---

The differentiator is shipped, not described, and that's what separates this from most factories on paper: five end-to-end samples with their intermediates, and provenance sidecars whose inference counts actually match each lane's declared posture — the educational guide carries exactly one flagged inference, the SOP carries zero because its lane bans them. The $75 detail is the build in miniature: the screen shows a number, the narration says policies vary, and the SOP declines to assert the number. That restraint is the lane. The inference spectrum as the axis a lane is designed on — a guide may infer order, an SOP may infer nothing, and the audit checks exactly that — is an original contribution; so is pinning the manifest schemas and shipping a real test that proves the pinning.

The push is small and sits at your front door: the README still calls the SOP lane a documentation-only bonus while your CONTEXT file, the lane's own rules, and your commit message all say it's wired with a shipped sample — staleness that taxes trust exactly where a stranger starts reading. Nicolás's sync rule is the fix: name one file canonical and make disagreement a bug. Then close the brand seam so /setup actually re-skins the render, and the factory claim is airtight.

---

An idea worth naming, and it is yours: the inference spectrum as the lane-design axis — how much a document type may infer is the property that defines the lane, and the QA gate audits exactly that. It's the difference between a guide that helps and an SOP that lies.

Worth a look:
- [Nicolás Patrón — structure-call-ar](https://github.com/Nicopatron/structure-call-ar) — the canonical-copy rule: when two files disagree, one is defined as right and the other as a bug
